
<%= form_for [@order, @payment] do |f| %>

	<%= f.hidden_field :order_id, value: @order.id %>

	<%= f.label :amount, 'Valor' %>
	<%= f.text_field :amount  %>

	<%= f.label :date, 'Fecha' %>
	<%= f.text_field :date  %>

	<%= f.submit %>


	<% end %>
